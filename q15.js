let isRaining = true;
let isSnowing = false; 
let windSpeed = 16; 
let isCloudy = true; 
//
if (isRaining === true && isSnowing === false && windSpeed > 15 && isCloudy === true) {
    console.log("SUCCESS!! The weather is good for launch!");
} else {
    console.log("FAIL!! The weather is NOT good for launch");
}